name: Code Linter

on:
  pull_request:
  merge_group:
    types: [checks_requested]
  workflow_dispatch:
    inputs:
      mode:
        description: Files to scan
        required: false
        default: all
        type: choice
        options:
          - all
          - changed
  schedule:
    - cron: "0 5 * * 1"

permissions:
  contents: read

concurrency:
  group: code-linter-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  code-linter:
    uses: ForkHorizon/ci-gates/.github/workflows/code-linter.yml@main
    with:
      mode: ${{ inputs.mode || 'auto' }}
      config: .code-linter.json
      runs-on: '["self-hosted", "macOS", "ARM64", "ci-scope-broker"]'
